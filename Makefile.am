AUTOMAKE_OPTIONS = foreign
CFLAGS += -ggdb -Wall -march=native
CXXFLAGS += -ggdb -Wall -march=native
LDFLAGS += -lpthread -lz -lm

bin_PROGRAMS=epi-emmax emmax-kin

epi_emmax_SOURCES = epi-emmax.c

emmax_kin_SOURCES = emmax-kin.c
